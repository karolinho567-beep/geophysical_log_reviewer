# Packaging

Platform-specific distribution tooling lives here. Application behavior belongs
in `src/logcv`; packaging code only turns that installed product into a distributable
artifact.

Generated files are written to the project-level `build/` and `dist/` folders.
